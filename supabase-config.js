// Weekender — Supabase config
//
// Paste your project's URL and anon key below to enable shared sync.
// Find them in your Supabase dashboard: Settings → API.
// The anon key is designed to be public; row-level security is what
// guards your data. See supabase/schema.sql.
//
// Leaving these empty makes the app fall back to local-only mode
// (state lives in this browser's localStorage, no sync with friends).

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
